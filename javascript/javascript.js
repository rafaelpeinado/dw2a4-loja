/*function adicionarCarrinho(id) {
    $.getJSON('json/loja.json', function (data) {
        $.each(data, function (i, livro) {
            if (livro.id == id) {
                var qtde = parseInt($('#qtdeCart').text()) + 1;
                $('#qtdeCart').text(qtde);
                return true;

            }
        });
    });
}*/